#!/usr/bin/env python3
"""
"""


def index_range(page: int, page_size: int) -> tuple[int, int]:
    """
    """
    for num in range(page):
        index = num * page_size

    pages = page * page_size

    return (index, pages)
